import styled from "styled-components";

export const HeaderComponent = styled.header`
    width: 100%;
    height: 55px;
    display: flex;
    justify-content: left;
    align-items: center;
    padding-left: 41px;

    box-shadow: 0px 1px 60px -20px rgba(0,0,0,0.51);
`